# Rusty Audio Auth Service - Environment Variables
# Copy this file to .env.local for local development
# For production, set these as Cloudflare Worker secrets

# =============================================================================
# OAuth Provider Credentials
# =============================================================================

# Google OAuth 2.0
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# GitHub OAuth App
# Get from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Microsoft Azure AD / OAuth
# Get from: https://portal.azure.com/ (App registrations)
MICROSOFT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret

# =============================================================================
# JWT Configuration
# =============================================================================

# JWT signing secret (IMPORTANT: Use a strong, random value)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_min_32_characters_random_string

# =============================================================================
# Application Configuration
# =============================================================================

# Application URL (where your WASM app is hosted)
APP_URL=http://localhost:8080

# OAuth redirect URI (must match OAuth provider settings)
REDIRECT_URI=http://localhost:8080/auth-callback

# Environment
ENVIRONMENT=development

# =============================================================================
# Cloudflare KV Namespace IDs (Set in wrangler.toml)
# =============================================================================

# Get these from: wrangler kv:namespace create <name>
# SESSIONS KV namespace ID
# USERS KV namespace ID
# RATE_LIMIT KV namespace ID

# =============================================================================
# Production Environment Variables
# =============================================================================

# For production deployment, update these values:
# APP_URL=https://rusty-audio.pages.dev
# REDIRECT_URI=https://rusty-audio.pages.dev/auth-callback
# ENVIRONMENT=production

# Set production secrets with:
# echo "<value>" | wrangler secret put GOOGLE_CLIENT_ID --env production
# echo "<value>" | wrangler secret put GOOGLE_CLIENT_SECRET --env production
# echo "<value>" | wrangler secret put GITHUB_CLIENT_ID --env production
# echo "<value>" | wrangler secret put GITHUB_CLIENT_SECRET --env production
# echo "<value>" | wrangler secret put MICROSOFT_CLIENT_ID --env production
# echo "<value>" | wrangler secret put MICROSOFT_CLIENT_SECRET --env production
# echo "<value>" | wrangler secret put JWT_SECRET --env production
